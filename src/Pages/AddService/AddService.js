import React from 'react';

const AddService = () => {
    document.title = "Add Service";
    return (
        <div>
            <p>add a new service</p>
        </div>
    );
};

export default AddService;